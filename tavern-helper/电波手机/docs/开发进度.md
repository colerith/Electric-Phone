# 电波手机开发进度

更新日期：2026-09-12

## 已完成：第一里程碑（可运行 MVP）

- [x] 纯脚本项目入口：只有 `index.ts`，无 `index.html`。
- [x] Vue + Pinia 挂载到酒馆父页面，支持重复加载清理与 `pagehide` 卸载。
- [x] 电波机蓝粉渐变视觉、悬浮入口、手机壳、主屏和移动端适配。
- [x] 状态、消息、备忘、空间、钱包、日历、浏览、音乐 8 个 App 入口与快照展示。
- [x] `<角色手机>`、`<charN>` 与 8 类 App 标签解析，支持跨楼层快照合并。
- [x] 角色卡 → 聊天文件 → Char 三级隔离；单人卡幂等创建默认 Char。
- [x] 多人/群组模式禁止用容器卡名自动创建 Char；同名歧义保持待确认隔离身份。
- [x] 当前聊天变量保存线程、草稿、消息和 App 状态；脚本变量只保存全局设置与副 API。
- [x] 私聊会话列表、聊天气泡、发送/失败/停止/删除状态。
- [x] 文本、表情、照片、视频、语音描述、虚构转账、位置、链接和剧情通话消息入口。
- [x] 独立副 API：OpenAI-compatible、Google AI Studio、Vertex AI，含连接测试、超时和一次 JSON 修复。
- [x] 结构化 PhoneChatResponse 校验；切换聊天后丢弃旧请求结果。
- [x] 8 个 App 模块提示词与跨 App 路由提示词；未引入 nuopreset marker 或框架包装。
- [x] 电波机 `资源` 目录完整复制到 `assets/reference/electric-wave`，参考截图不进入运行包。
- [x] `pnpm build` 生产构建通过；解析器与 schema 烟雾测试通过。
- [x] 从电波机迁移并脚本化组件规范，新增 `WaveSelect`、`WaveToggle` 与 `WaveImageUpload`。
- [x] 禁用业务模板中的 h1-h4、原生 select/radio/checkbox，并为所有滚动区域隐藏系统滚动条。
- [x] 以 `#wave-phone-script-root` 建立主题隔离，重置酒馆主题容易污染的常见控件属性。
- [x] 首页新增 Char 信息入口；单卡自动读取角色卡头像，支持按角色卡与实际 Char 保存备注和自定义头像。
- [x] 头像编辑新增拖动选区、缩放、水平/垂直偏移与 24/40/58px 小头像实时预览，裁切参数随 Char 保存。
- [x] 头像轨道改为正圆；二级页状态栏与应用栏改为同背景沉浸式顶部区域。
- [x] 时间移至状态栏左侧，灵动岛仅保留装饰；设置入口升级为与 8 个应用一致的网格卡片。
- [x] 全量旧样式收进 `#wave-phone-script-root`，修复样式传送后污染酒馆主题的问题。
- [x] 移除悬浮入口与首页关闭按钮，自动建立并绑定“📱 电波手机”快速回复栏按钮进行显隐切换。
- [x] 新增独立 `WaveSlider`，原生 range 仅保留透明输入能力，轨道与滑块完全自绘。
- [x] 头像弹层限制在手机边界内并覆盖弱化顶部栏；修复私聊列表宽度、聊天底栏断层和消息二级面板视觉。
- [x] 设置页升级为四分类的两级结构：首页分类、API 设置、聊天与行为、语音与媒体、通知与音效。
- [x] 统一滑块、开关、扩展功能卡片与保存按钮的纯色组件样式，移除控件中的蓝粉渐变滥用。
- [x] 头像编辑区域改为不透明实色卡片；水平/垂直选区改用可见图层平移，并允许保存酒馆 `/characters/...` 内部头像路径。
- [x] 头像修改器升级为手机内正式子页面，并按缩放量约束水平/垂直位移，杜绝头像边缘漏底。
- [x] 设置新增“字体与显示”：系统字体、思源宋体、思源黑体、UI 整体字号和 400–900 展示字重。
- [x] Home 指示条改为透明悬浮沉浸式样式；消息扩展功能区移除卡片与图标底板。
- [x] 新增长按/右键消息菜单：编辑、复制、收藏、多选、撤回、引用、转发和删除，并实现批量操作。
- [x] 文字、表情、图片、视频、语音、转账、位置、链接、通话与系统消息使用各自独立美化组件；发送前自动收起交互弹层。
- [x] 消息多选使用独立异形复选控件，并支持以首条消息为锚点“选到这里”、回到起点与跳到线程末尾。
- [x] Char/User 普通气泡与语音分别使用低饱和蓝/粉；媒体、位置、链接、通话保持中性表面并只改变局部强调色，位置消息使用消息种子动态绘制 CSS 地图。
- [x] 按原电波机色卡修正消息颜色语义，转账改为轻票据样式，并修复语音波形与视频播放图标的继承错位。
- [x] 剧情语音默认折叠转写、按文本长度自动换算时长，并提供独立播放波形动画。
- [x] 虚构转账支持未收款、已收款、已退款三态，状态字段与生成提示词、钱包规则保持一致。
- [x] 新增 Emoji 独立分类和表情包资源库：分类、新建、改名、收藏、多选删除、图链/文件批量导入、文本导出以及全局/User/Char 使用范围。
- [x] 表情库默认留空，移除“下班小狗”内置资源；迁移保留用户自建内容，Char 专属资源按实际 Char key 过滤。

## 下一里程碑

- [x] 七个非消息 App 已挂载各自基础界面；统一改用灰白中性表面。
- [x] 状态页拆出 `WaveStatusPanel`：档案、双指标、情绪标签、心声与器官分组；修正小数、变化说明及多行解析。
- [x] SwiftUI 启发的统一规范写入组件规范 1.2，取代旧版彩色页面底与非对称圆角。
- [ ] 继续逐个精修其余六个 App；状态页需在实际酒馆完成视觉验收。
- [ ] 增加本地媒体文件持久化配额、资源缺失重试与孤立资源清理策略。
- [ ] 扩展 Char 信息页，用于同名临时 Char 的人工确认、合并和拆分。
- [ ] 增加副 API 模型列表获取、多配置槽和提示词逐模块编辑/恢复默认。
- [ ] 在实际 SillyTavern 页面进行桌面/移动端视觉 QA、控制台检查和交互回归。

### 2026-09-12 状态面板验证

- 状态解析检查通过：小数、正负变化说明、多行心声、括号与冒号器官格式、空数据、数值边界和 HTML 字面量。
- 新状态组件与 App 容器 ESLint 通过；电波手机生产构建通过（包体积提示）。
- 独立组件预览构建通过；浏览器连接为空，启动本地无头 Chrome 截图被自动审批拒绝，未宣称完成截图或实机验收。

### 消息与备忘迭代

- 视频改为独立播放区与描述流式布局；保留暂停后的媒体控件，补充加载/播放失败反馈。
- 照片改为拍立得白边卡片，普通文字气泡按最新参考恢复单侧小直角。
- 私聊补齐双方头像，User 头像同步当前 persona；头像与消息首行对齐。
- 手机名称展示统一优先使用备注；原始身份键、卡名匹配与生成寻址保持稳定。
- 备忘挂载独立纸张/便签/涂鸦票据 UI，支持搜索、分类和展开，保留 ASCII 空白与多行解析。
- 解析与备注优先级检查、ESLint、生产构建通过。浏览器扩展未连接，实际视觉与交互验收仍待完成。

### 单条消息、表情与空间迭代

- 消息操作菜单挂在对应消息上方，编辑、收藏、复制和引用按消息 ID 操作；支持编辑双方消息，撤回仍限本人。
- 统一 11 种消息类型的提示词序列化，主 API 桥接、追加输入和副 API 共用；引用带原文与 ID，媒体编辑同步描述字段。
- 表情添加改为设备内独立弹窗，支持名称、链接、批量、文件、分类与作用域；移除内置资源并兼容旧库迁移。
- 空间挂载独立资料与日记组件，灰白页面、生成资料标签、搜索、收藏式点赞筛选、长文展开与评论区。
- 点赞和评论按角色/动态持久化；动态可发送到聊天，保留作者、日期和正文。更新空间与评论回复接入已配置副 API。
- AI 资料按实际返回字段合并，动态与评论保留稳定 ID；切换角色取消生成并丢弃旧响应。
- 模拟请求检查覆盖三条发送路径、所有类型、引用、单条编辑、持久化、空间生成与转发、角色切换隔离；通过。ESLint 与生产构建通过（体积警告）。未调用真实 API；浏览器未连接，尚未完成实机视觉验收。

### 空间与相册补充

- 空间双处头像同步统一裁切；空间社交昵称优先展示 AI username，首页空间图标改用可用的书本图标。
- 二级页三横杠菜单打开应用功能设置；空间 AI 更新迁至顶部，封面点击设置链接并可恢复默认渐变。
- 修复引用在 AI 失败后残留；增加引用与转账卡表面对比，单图拍立得日期底边，多图选择、描述、移除及堆叠展开。
- 扩展模拟检查覆盖相册逐图格式化、封面保存与重置；生产构建通过。浏览器未连接，未完成实际页面视觉验收。

### 设置与钱包实装验证

- 补充全局 SCSS 声明，TypeScript 检查无电波手机 SCSS 导入诊断；全仓仍有其他声明文件既有错误，未宣称全仓类型检查通过。
- 独立 WaveAppSettings 和 WaveWalletPanel 已挂载；封面渐变明确绑定，修正重复句号，引用正文宽度独立，媒体与相册样式统一。
- 内置两张 image_gen 图案，WebP 随脚本内嵌；空间/钱包支持图案切换及图片组件图链、文件上传，按角色持久化。
- 钱包结构化数据与旧文本兼容，支持记账、十分类、搜索、统计，AI 合并保留既有记录。
- 钱包分类、交易去重、保留余额、待确认排除、手工记账、卡面偏好持久化及此前交互模拟检查通过；ESLint 和生产构建通过（包体积警告）。未调用真实 AI 服务，尚未完成实机视觉验收。

### 日历与图库重构验证

- image_gen 独立生成空间 5 图与银行卡 5 图，原图保存到 assets/zone、assets/wallet，内嵌 WebP；旧版共享卡面自动映射到新卡面，图库入口已分离。
- 日历替换原月历占位，接入天气、五日导航、时间轴及日期筛选；天气位置搜索和清除接入脚本设置持久化。
- 上海真实城市搜索与天气请求成功，保存返回样本验证数据结构及单位。模拟检查覆盖缓存、强制刷新、取消、HTTP 错误、多城市候选、零值/缺失值、日程重要/完成解析和图库隔离；原消息/钱包交互回归通过。
- 封面铺满、相册正常流占位、底部照片按钮与表情框内描边已修复。ESLint、生产构建通过（内嵌 10 张新图使包体积增加，有性能提示）；TypeScript 无电波手机诊断，全仓仍有其他声明文件既有问题。
- CUA 当前 apps/browsers 均为空，未完成实际酒馆页面视觉验收；真实 API 联通测试不等同于浏览器视觉验收。

### 2026-09-12 浏览器与交互修复
完成应用设置滚动、天气地名编辑、位置距离与直立图标、发送按钮对比度；新增浏览器首页、真实外部搜索、按角色历史记录与收藏、网页转发。生产构建和模拟交互测试通过，未调用角色 AI API；浏览器视觉复核待可用会话。

### 2026-09-13 浏览器结果与音乐播放器
- 浏览偏好按标题、引擎、手机内接口、历史管理分组留白；可设置 SearXNG 的 /search JSON 端点。默认使用维基百科结果，明确来源，不伪装成 Google 实时结果。结果支持逐条收藏与转发，失败保留外部搜索入口。
- 重构音乐页：磨砂唱片与唱臂、歌曲信息、同步歌词、可拖动进度、上一首/下一首、单曲循环、歌曲收藏和搜索列表。首页原小头像栏替换为共用播放器的“正在听”。
- 参考用户提供播放器的搜索/详情/歌词分离方式，未执行其脚本或远程 Music.js。默认 VKeys 网易云/QQ 音源，保留 GDStudio 兼容接口配置；缺失歌词尝试 LRCLIB 精确歌名/歌手匹配。
- AI 使用“歌曲名称 / 歌手名称 / 听歌感想”格式；自动检索并只在匹配时载入，不自动播放，不伪造歌曲时长或歌词。
- 验证：生产构建、ESLint、模拟歌曲搜索/详情/LRC/播放状态/角色切换/收藏保存/结果 URL 过滤测试通过。VKeys 搜索和详情实测 HTTP 200（含封面、音源地址）；GDStudio 和维基百科在当前环境返回 403，未声称所有来源可用。尚未进行实际浏览器听音和视觉验收。
- 协议参考：https://docs.searxng.org/dev/search_api.html 、https://www.mediawiki.org/wiki/API:Search 、https://lrclib.net/docs 。

### 2026-09-13 音乐分层与语音合成
音乐主页独立呈现搜索、角色电台、收藏曲目与底部迷你播放器；播放页使用独立沉浸顶栏。放大唱片封面、减少纹路，以 SVG 绘制转轴/弯折/针头。进度使用 WaveSlider 的独立轨道样式，标题不使用宿主 h3 装饰。歌词依照音频 currentTime 高亮，手动滚动暂停跟随，通过“回到当前歌词”恢复。播放背景光效仅播放时运动，支持减少动态效果偏好。
语音与媒体页移除自动时长换算说明，加入 MiniMax 国内/海外及 ElevenLabs 的开关、密钥、模型与代理地址。角色聊天设置独立存储 provider、voiceId、speed、pitch/style/stability；MiniMax 使用 pitch，ElevenLabs 使用真实支持的 style/stability，不发送虚构 pitch 参数。语音气泡点击时合成并播放真实 MP3，加载后显示音频实际时长；失败提示保留文字转写，卸载取消请求并释放 Blob URL。
验证：生产构建与 ESLint 通过；模拟测试覆盖区域端点、鉴权映射、MiniMax hex 解码、服务失败、角色配置保存，以及音乐 seek/currentTime/LRC 高亮。没有调用付费 TTS，没有完成浏览器视觉及实际听音验收。
官方协议：
- https://platform.minimaxi.com/docs/api-reference/speech-t2a-http
- https://platform.minimax.io/docs/api-reference/speech-t2a-http
- https://elevenlabs.io/docs/api-reference/text-to-speech/convert


### 2026-09-13 日推、歌单与一起听
音乐主页内置六首精选曲目，点击后通过配置音源检索匹配版本；无资源时保留错误提示。喜欢按钮统一红心，收藏目录不再被搜索记录数量截断。歌单分类和独立播放队列保存在聊天变量，重启可恢复，不自动播放；歌曲 ＋ 可加入队列或歌单，底部迷你播放器和播放页均可打开队列并移除曲目。
唱针绕转轴平滑落针/收回；一起听组件在播放页及聊天页显示双头像、曲目和实际播放计时。灵动岛支持展开、跳转、播放/暂停及下一首，声波只在播放时运动。发送到主 API、追加输入和副 API 的消息均附加当前角色对应的曲目状态。
验证：模拟回归覆盖歌单去重、收藏/队列持久化、搜索不覆盖队列、三种发送路径的听歌上下文、播放/暂停状态；生产构建与 ESLint 检查。尚未完成连接酒馆后的视觉及实际听音验收。


### 2026-09-13 歌单详情与在线推荐
歌单独立置于“喜欢的歌”右侧标签；行显示封面、名称和数量，左滑、右键或更多按钮显示删除操作。详情页显示大封面及歌曲列表，右上角编辑名称/封面，复用图片上传组件。旧歌单 cover 默认空串，以首张歌曲封面回退。
推荐服务支持 NeteaseCloudMusicApi 的 personalized/newsong、recommend/songs，以及 jsososo/QQMusicApi 的 new/songs、recommend/daily。默认公开新歌精选；启用登录态后才携带服务 Cookie 请求个性化日推，失败回退公开精选，再失败显示固定歌曲。网易云默认兼容服务实测 200、Access-Control-Allow-Origin: *；QQ 公开实例连接失败，需填写可用兼容服务地址。官方域名返回有效 JSON 但没有 CORS，因此没有将其作为浏览器默认请求地址。
歌词新增网易云/QQ 兼容接口，保留现有音源和 LRCLIB 回退。OpenCC t2cn 随脚本打包，本地简体转换不修改时间戳。灵动岛显示歌曲封面、滚动标题/歌手、实时歌词、可拖动进度与上一首/播放/下一首。聊天 app 偏好首页直接显示角色语音服务、Voice ID、语速和语调模块。位置图钉改成完整竖直 SVG，去掉斜角外框。
验证：歌单改名/封面/删除持久化、简体转换、网易云/QQ 数据映射、日推回退、登录态仅明确启用时携带的模拟测试通过。未进行酒馆浏览器视觉和实际听音验收。
接口依据：https://github.com/jsososo/QQMusicApi/blob/master/routes/new.js 、https://github.com/jsososo/QQMusicApi/blob/master/routes/recommend.js 、https://github.com/jsososo/QQMusicApi/blob/master/routes/lyric.js 、https://github.com/tal007/netease-cloud-music 、https://github.com/nk2028/opencc-js 。


### 2026-09-13 控件修复与统一沉浸外壳
歌单标签改为“我的歌单”。收藏/队列弹窗改为注入手机 DOM 目标，修复跨 iframe 的 Teleport 查询失败。手机状态栏/导航栏统一透明背景，歌单详情封面铺至设备顶部且隐藏重复音乐标题；上传封面编辑控件缩为横向预览，返回/播放改用图标。修复开关在纵向 flex 容器中因 flex-basis 导致高度错误。灵动岛仅在真实文字宽度溢出时滚动。
验证：生产构建、ESLint、TypeScript 手机目录无诊断；新增实际 Vue + 双文档 DOM 组件测试，覆盖点击歌曲收藏、打开/关闭队列、入队、歌单详情进入/返回、短标题静止与长标题溢出，以及开关尺寸/状态/滑块样式。既有业务回归通过。DOM 测试不等同于酒馆浏览器的视觉验收。


### 2026-09-13 常驻音乐控制与可排序队列
移除音乐页面内嵌迷你播放器，由 app 外壳统一挂载底部 Dock；真实播放后常驻，暂停保留，完整播放页隐藏，各子页面预留底部空间。播放列表拆为紧凑 WaveMusicQueue，支持顺序/随机/列表循环/单曲循环、移除/清空、触屏与鼠标拖动及方向键排序。模式和顺序持久保存，歌单详情新增独立喜欢按钮与入队反馈。
验证：实际 Vue 双文档组件测试覆盖弹窗、入队、指针拖动、键盘排序、模式选择；业务测试覆盖顺序末尾停止、循环回首、随机排除当前、单曲自然结束/手动切歌区分，以及队列顺序和模式保存。未进行酒馆浏览器视觉验收。


### 2026-09-13 聚合音乐源
按用户提供地址接入 GDStudio 网易云、VKeys QQ、VKeys 网易云和 Meting 酷狗，去重重复详情地址。默认聚合搜索并支持逐源选择；轮询分段呈现，失败隔离、取消和供应商来源持久化。VKeys 的详情、GDStudio URL/LRC、Meting song/远程LRC均按原歌曲来源路由。队列统一手机主题配色。
验证：四源顺序/查询参数编码、来源与 ID 保留、详情/LRC路由、Meting hash 提取、持久化、部分失败、全部失败和取消的模拟测试通过；Vue组件回归、ESLint和生产构建通过，TypeScript手机目录无诊断。公开搜索实测 VKeys 两源 200/CORS *，GDStudio与Meting返回403，不声称所有远程源可用。未进行酒馆内视觉验收。

## 2026-09-14：Dock 与双触发生成初版
- Dock 采用三列、54px 图标；推特鸟占位图标新增至资源库、运行内嵌表及外观编辑列表。
- 新增 module-protocol.ts / follow-generation.ts，接入单次主请求注入、随机模块选择、HTML 卡片输出和专用全局正则幂等安装。
- 新增手动模块生成入口与设置；新协议可重放同步到快照与消息线程。
- 初版边界与后续事项见需求文档“2026-09-14”章节；当前没有迁移全部旧私聊/空间生成按钮，也没有批量重写旧楼层。

验证：运行项目根目录下 `node src/util/酒馆助手脚本/电波手机/tests/module-generation.cjs` 与 `node src/util/酒馆助手脚本/电波手机/tests/module-store.cjs`。覆盖概率边界、JSON 校验与坏数据拒绝、HTML 转义/去重、提示词剥离、全局正则幂等保留、跟随注入/清理、实际 Pinia 重复同步、swipe 替换、删除楼层、手动模块请求写回与切换聊天丢弃结果。使用模拟酒馆 API，未调用真实模型，也未完成酒馆实机联调。

## 2026-09-14：消息应用重做
- 新增 WaveMessenger.vue、messenger.scss：四导航、头像摘要列表、左滑/右键会话操作、加号菜单与通讯录/群聊弹窗。
- 新增 narrative-context.ts：私聊关系模式、模型 context_relation、群成员提示与主请求上下文参考。
- 跟随生成新增必更新模块，与随机组互斥；移除设置中的手动生成区。
- 已验证：实际 Vue + Pinia 的菜单、Teleport 弹窗、创建联系人/群聊、同步保持、置顶/移除/恢复、两组选择、概率边界；群聊成员校验与独立模式请求/记录标记，既有模块生成回归。
- 验证命令：`node src/util/酒馆助手脚本/电波手机/tests/messenger.cjs`。浏览器截图启动被自动审批以 blocked by policy 拦截，未完成像素级视觉验收；未请求真实模型。
