# 仿手机 AI 聊天 Web 软件需求文档

> 文档状态：产品需求初稿（PRD v0.1）  
> 更新日期：2026-08-25  
> 目标形态：移动端优先、兼容桌面浏览器的前后端 Web 应用，可进一步封装为 PWA  
> 视觉基准：以 [`资源/UI参考图`](./资源/UI参考图/) 中的例图为准

## 1. 产品概述

### 1.1 产品定位

产品是一套“仿手机系统 + AI 角色社交”的可配置 Web 软件。用户可创建多个 `User`（用户身份）与 `Char`（AI 角色），为双方绑定 NPC、模型、世界设定、记忆与外观，并通过接近真实手机社交软件的界面进行聊天、通话、媒体分享和娱乐互动。

产品应将 AI 模型、生图、语音、音乐、向量数据库和 MCP 等能力封装为可替换的外部服务，核心业务不绑定单一供应商。

### 1.2 核心价值

- 高沉浸：时间、时区、日程、钱包、空间、音乐和“手机内容”共同塑造角色生活感。
- 高可定制：从角色设定、提示词、记忆到图标、壁纸、字体、音效均可配置。
- 高兼容：通过统一连接器兼容 OpenAI-compatible 及其他主流 API。
- 短聊天优化：通过聊天补全 Preset、世界书与记忆注入，让模型输出更像即时通信，而非长篇角色扮演。
- 数据可控：角色、聊天、记忆、预设和美化方案均支持导入、导出与备份。

### 1.3 目标用户

- 希望与 AI 角色进行沉浸式陪伴或角色扮演的用户。
- 需要细粒度管理角色卡、世界书、提示词和模型参数的进阶用户。
- 希望制作并分享主题、美化预设、角色卡或聊天模板的创作者。

## 2. 产品原则与边界

### 2.1 产品原则

1. 移动端优先，核心聊天链路在单手操作下完成。
2. API 可替换，密钥与供应商细节不得渗透到业务数据模型。
3. 角色行为可追溯，用户能查看本次生成使用了哪些记忆、世界书和 Preset。
4. 高风险能力默认收敛：HTML 渲染、链接解析、MCP 工具调用、媒体上传均需沙箱与权限控制。
5. 配置复杂度渐进呈现：普通用户使用默认值，进阶项折叠到高级设置。

### 2.2 首版边界

- “红包/转账/钱包”仅为虚拟资产系统，不接入真实支付或法币结算。
- 视频通话首版可用“实时语音 + 角色立绘/口型/状态动画”模拟；真实双向视频可作为后续能力。
- “偷看 Char 的手机”是由角色设定与生成内容驱动的虚构功能，不读取真实设备或第三方账户数据。
- HTML 美化仅渲染经过清洗的受限 HTML/CSS，不执行任意 JavaScript。
- 自动解析分享链接只读取公开元数据，不绕过登录、付费墙或访问控制。

## 3. 名词与核心对象

| 名词 | 定义 |
| --- | --- |
| User | 用户创建的自我身份，可有独立昵称、头像、人设、时区与钱包 |
| Char | 可聊天的 AI 角色，可有独立角色卡、模型、声音、记忆和外观 |
| NPC | 参与世界但通常不作为当前主聊对象的人物，可绑定给 User 或 Char |
| Conversation | 一段单聊或群聊会话，持有独立美化、世界状态和记忆策略 |
| Message | 统一消息实体，类型可为文本、图片、语音、转账、位置等 |
| Provider | 外部 API 供应商配置，如模型、生图、TTS、STT、音乐、Embedding |
| Preset | 提示词模板、上下文拼装规则、采样参数与输出约束的组合 |
| 世界书 | 按关键词、条件、优先级和位置注入上下文的知识条目集合 |
| 记忆 | 原始对话、摘要、事实、事件与向量索引组成的长期上下文系统 |
| 美化预设 | 图标、壁纸、字体、声音、气泡和布局等主题资源的成组配置 |

## 4. 信息架构

### 4.1 手机层

- 锁屏：时间、日期、通知摘要、小组件、壁纸、解锁入口。
- 桌面：App 图标、文件夹、Dock、小组件、分页与布局编辑。
- 全局设置：账户与数据、API、主题、字体、音效、备份、安全和开发者选项。

### 4.2 核心 App

- 消息：会话列表、聊天、通讯录、表情包、通话记录。
- 角色中心：User、Char、NPC 的创建、绑定、导入导出和关系管理。
- AI 设置：Provider、模型、Preset、世界书、记忆与向量化。
- 个人空间：参考 iCity 的动态、相册、留言、收藏与装扮。
- 音乐：搜索、播放列表、一起听、歌词和角色互动。
- 日程：User/Char 日历、事件、提醒、时区与由 AI 建议的安排。
- 钱包：虚拟余额、红包、转账、礼物和流水。
- Char 手机：经角色设定生成的相册、备忘录、搜索记录、聊天和 App 状态。

## 5. 功能需求

### 5.1 AI 设置与连接

#### 5.1.1 Provider 管理

- 新增、编辑、复制、启用、禁用和删除 Provider。
- 类型至少包括：聊天补全、Embedding、生图、TTS、STT、音乐、内容审核、重排序。
- 支持 OpenAI-compatible 的 `baseUrl`、`apiKey`、模型名、自定义请求头、组织 ID 和代理配置。
- Provider 能力探测：拉取模型列表、测试连接、展示延迟与错误详情。
- API Key 在服务端加密保存；前端读取时仅显示掩码，不通过日志输出。
- 可设置全局默认、按 Char 默认、按 Conversation 覆盖以及失败回退链。
- 支持流式输出、中止生成、重试、继续生成和重新生成。
- 记录调用用量、耗时、模型、错误码和估算成本，不记录明文密钥。

#### 5.1.2 模型参数

- temperature、top_p、max_tokens、frequency/presence penalty、seed、stop 等常用参数。
- 参数能力由 Provider schema 动态声明；不支持的字段不发送。
- 支持模型参数方案的保存、复制、导入导出和按场景切换。

### 5.2 User、Char 与 NPC

- 创建多个 User 与 Char，并在进入会话前选择当前 User 身份。
- User 字段：昵称、头像、简介、人设、性别/称谓（可选）、生日（可选）、时区、语言、钱包与隐私设置。
- Char 字段：名称、头像/立绘、角色卡、开场白、示例对话、性格、背景、关系、时区、语言、语音、模型与记忆策略。
- NPC 字段：名称、头像、简介、与 User/Char 的关系、出场条件、可见范围。
- 支持多对多绑定：一个 NPC 可关联多个 Char/User；绑定关系包含称呼、亲密度和关系说明。
- Char/User/NPC 支持标签、搜索、排序、复制、归档、导入导出。
- 删除前展示影响范围；默认软删除并允许从回收站恢复。
- 角色卡格式应预留与常见 SillyTavern 角色卡/JSON 的映射层。

### 5.3 聊天与消息

#### 5.3.1 会话

- 首版支持 User 与 Char 单聊；数据模型预留群聊与多 Char 参与者。
- 会话列表显示头像、名称、最后消息、时间、未读数、置顶、免打扰和草稿。
- 支持新建、搜索、归档、删除、置顶、标记未读和批量管理。
- 会话可单独指定模型、Preset、世界书、记忆、时区与美化方案。

#### 5.3.2 消息类型

统一 `Message` 需至少支持：

- 文本、受限 HTML 富文本、表情包。
- 真实图片/视频上传、URL 媒体、AI 生图结果。
- 语音消息、语音转文字、视频/语音通话事件。
- 位置、链接卡片、文件（后续）、引用与合并转发。
- 红包、转账、礼物、系统提示、拍一拍。
- 正在输入、已发送、失败、已读/未读、撤回等状态。

所有消息使用统一外壳和类型载荷，例如 `type + contentJson`；消息类型修改后保留修改前版本和审计记录。

#### 5.3.3 发送与生成

- 用户可发送文本、真实图片、表情包、位置、礼物、红包、语音及链接。
- 支持多图发送，首版上限建议 9 张；上传前压缩并保留查看原图能力。
- 图片/视频弹窗支持描述文字、URL 与本地上传。
- 链接粘贴后自动识别并生成标题、描述、站点名、缩略图和安全状态；解析失败时仍可发送普通链接。
- AI 回复支持流式展示、停止、重新生成、继续生成与切换候选回复。
- 生成失败不能吞掉用户消息，应提供错误详情、重试和切换回退模型。
- 短消息 Preset 应约束回复长度、拆分条数、发送间隔、口语风格与表情使用频率。

#### 5.3.4 消息操作

- 长按/右键菜单：编辑、复制、收藏、多选、撤回、引用、转发、删除。
- 多选模式：批量删除、转发、收藏；离开前确认未执行的选择。
- 编辑与撤回保留版本记录；聊天界面只显示最新状态。
- 支持修改消息类型，仅允许 schema 可转换的类型或由用户补齐必填字段。
- 支持修改头像；会话头像与角色全局头像可分别设置。
- “拍一拍”可配置动作文本、音效、冷却时间和是否触发 AI 反应。
- 长按头像显示“心声”：由独立提示词生成，默认不写入公开对话；用户可选择保存到记忆。

#### 5.3.5 红包、转账和礼物

- 红包支持金额、祝福语、状态（待领取/已领取/退回/过期）和领取事件。
- 转账支持金额、备注、接收/拒绝/退回；金额只影响虚拟钱包。
- 礼物支持图标、名称、价格、动画、音效和亲密度影响。
- 所有钱包变动必须使用事务记录，重复请求通过幂等键防止重复扣款。
- 用户可为角色钱包设置初始值与是否允许 AI 自主消费。

#### 5.3.6 通话

- 语音/视频通话支持发起、接听、拒绝、挂断、静音、扬声器和通话历史。
- 实时语音链路支持 STT → LLM → TTS，并显示延迟/连接状态。
- 视频首版可展示角色图像、状态动效、字幕和语音波形。
- 通话结束后可生成摘要，是否写入记忆由会话设置控制。

#### 5.3.7 HTML 美化渲染

- 支持消息级受限 HTML 与主题级 CSS 变量。
- 使用白名单清洗标签、属性和 URL scheme；禁止 `script`、事件属性、iframe、任意表单和远程脚本。
- 富文本放入隔离容器，限制尺寸、定位、层级和网络访问，避免覆盖输入框或窃取数据。
- 提供“纯文本查看”和“本会话关闭 HTML 渲染”开关。

### 5.4 世界感知

- 时间模式：跟随真实设备时间、指定时区、固定自定义时间、按倍率流逝。
- User 与 Char 可处于不同时区，聊天页可显示双方当地时间、昼夜与“异地恋”提示。
- 将当前日期、星期、时段、时区差、节日（可选）以结构化变量注入模型。
- 可配置在线状态、睡眠时段、工作/上学时间和回复延迟模拟。
- 时间变化不应改写历史消息时间；所有时间在服务端以 UTC 保存，展示时转换。
- 日程事件可触发上下文变化、通知或角色主动消息，但必须可关闭。

### 5.5 记忆管理与自动总结

- 记忆分层：最近原文、阶段摘要、长期事实、重要事件、关系状态、向量召回片段。
- 自动总结可按消息数、token 数、会话阶段或手动触发。
- 摘要生成应保存来源消息区间、模型、提示词版本和生成时间。
- 用户可查看、搜索、新增、编辑、锁定、合并、删除和重新生成记忆。
- 锁定记忆不会被自动总结覆盖；删除原消息时提示其关联摘要/向量。
- Embedding Provider 可独立配置；支持批量向量化、重建索引和失败重试。
- 召回参数：top_k、相似度阈值、时间衰减、角色/会话过滤、重排序开关。
- 向量召回结果在调试面板中可见，可一键排除错误记忆。
- 敏感内容是否进入向量库应提供独立开关；导出数据时可选择包含或排除向量。

### 5.6 世界书（参考 SillyTavern 的使用理念）

- 世界书、分组、条目的增删改查、排序、复制、导入导出。
- 条目字段：标题、正文、关键词/次关键词、启用状态、优先级、扫描深度、概率、冷却、延迟、互斥组。
- 激活方式：常驻、关键词、正则、语义召回、时间/角色/会话条件。
- 注入位置：系统提示前后、角色设定前后、示例对话前后、聊天历史指定深度。
- 提供 token 预估、命中预览和冲突提示。
- 会话可组合多个世界书，并按全局、User、Char、会话四级覆盖。
- 仅借鉴交互与概念，具体实现和界面需原创，不复制第三方受版权保护的代码或素材。

### 5.7 聊天补全 Preset

- Preset 由提示词片段、顺序、角色、启用条件、模型参数、输出解析器组成。
- 支持系统提示、User/Char 描述、世界书、记忆、示例、历史消息和当前输入的可视化排序。
- 每个片段可启用/禁用、拖动、设定 token 上限与缺省策略。
- 短聊天控制项：单条字数、一次回复条数、是否拆句、表情频率、延迟范围、是否允许旁白/动作描写。
- 提供上下文预览、token 计数、最终请求 JSON 预览（隐藏密钥）和测试运行。
- 支持版本、复制、回滚、导入导出以及全局/Char/会话绑定。
- 输出解析失败时回退为普通文本，不丢失模型原始输出。

### 5.8 生图与语音 API

- 生图支持文生图、参考图、尺寸、数量、风格 Preset、负面提示词和重新生成。
- 可将 Char 外观描述自动拼入提示词；发送前允许用户预览和编辑。
- TTS 支持按 Char 绑定音色、语速、情感、语言；语音结果可缓存与重新生成。
- STT 支持录音转文字、用户确认后发送以及保留原始音频。
- 媒体任务使用异步队列，显示排队、处理中、成功、失败和取消状态。
- 供应商返回的媒体需转存到受控对象存储，避免临时 URL 失效。

### 5.9 外观美化

- 可自定义：App 图标、名称、锁屏、桌面布局、Dock、壁纸、聊天壁纸、气泡、字体、音效、小组件。
- 支持全局主题与单会话主题；单会话配置优先于全局。
- 美化预设可成组绑定上述资源，支持预览、应用、复制、导入、导出、恢复默认。
- 主题包需有 manifest，记录版本、作者、资源路径、颜色 token、兼容版本和校验值。
- 上传字体、图片、音频前校验格式、大小和来源；提供资源使用位置与删除影响提示。
- 支持浅色主题；深色主题作为后续里程碑，但颜色 token 从首版起预留。

### 5.10 娱乐与生活功能

#### 5.10.1 个人空间（参考 iCity 的内容组织感）

- User 与 Char 均可拥有空间主页、动态、相册、收藏、留言和装扮。
- Char 动态可由事件/日程触发生成；自动发布默认关闭，开启后可设审核模式。
- 动态可评论、点赞并影响关系状态；生成内容需标识为虚构角色内容。

#### 5.10.2 一起听音乐

- 接入音乐 API，支持搜索、播放、暂停、进度、歌词和播放列表。
- “一起听”同步房间状态，并将当前歌曲、歌词片段和播放进度提供给 Char。
- 无可用音乐 API 时允许使用用户上传或公开合法音频 URL。
- 记录音乐元数据，不在无授权情况下永久缓存受版权保护的完整音频。

#### 5.10.3 Char 的手机

- 可生成相册、备忘录、浏览/搜索记录、通话记录、日程和虚构联系人。
- 内容来自 Char 人设、世界书、记忆与近期事件；用户可刷新、锁定或删除。
- 明确标注为虚构生成内容，不读取真实设备数据。

#### 5.10.4 日程

- User/Char 分别拥有日历，支持事件、重复规则、提醒、参与者、地点和备注。
- Char 可基于人设建议或生成日程；写入前可要求用户确认。
- 日程参与世界感知，并可触发通知、主动消息或空间动态。

#### 5.10.5 MCP 服务设置

- 可配置多个 MCP Server，查看工具/资源清单、连接状态和权限。
- 工具权限按 Server、工具、Char、会话配置为禁止、每次询问、允许。
- 调用前展示参数与影响；写操作默认每次询问，结果写入审计日志。
- 密钥仅服务端保存；限制网络、文件系统与执行权限，防止提示注入越权。

#### 5.10.6 钱包

- 支持多个 User/Char 虚拟钱包、余额、流水、红包、转账、礼物和预算规则。
- 流水不可直接修改；纠错使用冲正记录。
- 可按角色配置收入来源与定期虚拟收入，但不得暗示真实金融价值。

## 6. 视觉与交互规范

### 6.1 固定视觉语言

根据 UI 例图，将默认主题固定为“清透电波 / Pink Electric Wave”：

- 产品主视觉语言、导航、按钮、功能名称、空状态、提示与默认示例内容统一使用简体中文。
- `Chat`、`ME`、`TA`、`World` 等仅可作为内部模块代号或面向国际化的翻译键；默认界面分别显示“聊天”“我的”“角色”“世界书”。
- `Setting`、`Theme`、`Preset`、`Memory` 默认显示“设置”“主题”“预设”“记忆”；确需保留的 API、MCP、HTML 等行业缩写可继续使用英文。
- 后续如支持其他语言，应通过完整的国际化资源包切换，不在同一层级随意混排中英文。

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--color-primary-700` | `#204075` | 主标题、主按钮、重要图标 |
| `--color-primary-400` | `#5E80BE` | 次级按钮、发送按钮、Char 气泡强调 |
| `--color-accent-pink` | `#FC9FBB` | 在线状态、User 气泡、选中态点缀 |
| `--color-ink-700` | `#374558` | 正文主色 |
| `--color-ink-400` | `#6E7682` | 次要信息、线性图标 |
| `--color-surface-muted` | `#F2F1F3` | 输入框、分段控件、弱背景 |
| `--color-surface` | `#FFFFFF` | 页面和卡片主体 |

- 大面积使用白色/近白色，蓝粉只承担品牌、气泡和关键操作，不高饱和铺满。
- 聊天背景可使用极淡粉蓝颗粒/云雾纹理，必须保证正文对比度。
- User 气泡使用淡粉，Char 气泡使用淡蓝；圆角约 16–24 px，可保留一角较小形成方向感。
- 主按钮深蓝底白字；次按钮使用白底或浅灰底；危险操作使用独立警示色，不能复用粉色。
- 图标优先 1.5–2 px 的圆润线性风格，避免混用拟物和粗实心图标。
- 弹窗使用白色卡片、轻阴影与背景模糊/暗化；移动端优先底部抽屉，关键确认可用居中弹窗。

### 6.2 字体

- 中文展示标题：思源宋体（Source Han Serif SC），建议 600/700。
- 中文正文与控件：思源黑体（Source Han Sans SC），建议 400/500/600。
- 英文装饰字仅用于金额卡片或小范围品牌装饰，不能承担重要信息。
- 回退栈：`Source Han Sans SC, Noto Sans CJK SC, PingFang SC, Microsoft YaHei, sans-serif`。
- 字体资源需确认可再分发许可；默认采用子集化 WOFF2 并设置 `font-display: swap`。

### 6.3 iOS 灵动岛与安全区

- 页面使用 `viewport-fit=cover`，所有固定头部/底部读取 `env(safe-area-inset-*)`。
- 产品界面不绘制或模拟灵动岛、刘海与手机外壳；仅在布局层面为真实设备预留安全区。
- 顶部交互元素不得进入状态栏/灵动岛区域；竖屏顶部安全内容起点建议不小于 `max(12px, env(safe-area-inset-top))` 加导航栏高度。
- 底部输入栏需叠加 `env(safe-area-inset-bottom)`，键盘弹起时保持输入框和发送按钮可见。
- 支持 320 px 到 430 px 常见手机宽度；桌面端以手机画布居中或使用响应式多栏布局。
- 使用 `100dvh`/Visual Viewport 处理 iOS 浏览器地址栏和软键盘，不依赖固定 `100vh`。
- 横屏、刘海屏和 PWA standalone 模式均需专项验收。

### 6.4 可用性与无障碍

- 常用点击目标不小于 44 × 44 CSS px。
- 正文默认不小于 16 px，辅助文字不小于 12 px。
- 文本与背景对比度目标达到 WCAG AA；仅装饰元素可例外。
- 颜色之外还需用图标/文本表达成功、失败、在线、选中等状态。
- 支持系统减少动态效果设置；音效默认可独立关闭。
- 表单具有明确标签、错误提示、加载状态和键盘焦点样式。

## 7. 建议技术架构

### 7.1 总体架构

建议采用模块化单体起步，保持未来拆分能力：

```text
Web/PWA 客户端
  ├─ 手机壳层与主题引擎
  ├─ 聊天/角色/空间/音乐/日程 UI
  └─ 流式消息、上传与实时通话客户端
          │ HTTPS / SSE / WebSocket
应用服务
  ├─ 鉴权与用户数据
  ├─ 角色/会话/消息/钱包
  ├─ 上下文编排（Preset + 世界书 + 记忆）
  ├─ Provider Gateway（LLM/图像/语音/音乐）
  ├─ 媒体与链接解析
  ├─ MCP Gateway 与权限审计
  └─ 后台任务与通知
          │
PostgreSQL + pgvector / Redis + Queue / S3 兼容对象存储
```

### 7.2 关键设计决策

- Provider 使用 Adapter 接口，业务层只依赖标准请求/响应对象。
- 消息使用不可变事件 + 当前投影：编辑、撤回、转账状态变化均留痕。
- 上下文编排器输出一份可预览的 `PromptEnvelope`，便于调试和复现。
- 媒体、生图、TTS、总结与向量化使用后台任务，避免阻塞请求。
- SSE 用于聊天流式输出；需要双向实时控制的通话与房间同步使用 WebSocket/WebRTC。
- PostgreSQL 存结构化数据；对象存储放图片/音频/视频；Redis 用于缓存、限流和临时状态。

## 8. 核心数据模型（概念级）

| 实体 | 关键字段 |
| --- | --- |
| `users` | id, owner_id, name, profile_json, timezone, avatar_asset_id, deleted_at |
| `characters` | id, owner_id, name, card_json, provider_binding, voice_binding, timezone |
| `npcs` | id, owner_id, name, profile_json |
| `entity_relations` | source_type/id, target_type/id, relation_json |
| `conversations` | id, current_user_id, title, theme_id, preset_id, memory_policy, time_mode |
| `participants` | conversation_id, entity_type/id, role, joined_at |
| `messages` | id, conversation_id, sender, type, content_json, status, created_at, deleted_at |
| `message_revisions` | message_id, revision, previous_content, reason, created_at |
| `providers` | id, type, encrypted_secret, base_url, capability_json, enabled |
| `presets` | id, name, version, prompt_nodes_json, model_params_json |
| `lorebooks` / `lore_entries` | 书与条目配置、触发条件、注入位置、优先级 |
| `memories` | id, scope, type, content, source_range, importance, locked |
| `memory_embeddings` | memory_id, embedding_model, vector, metadata |
| `assets` | id, owner_id, kind, storage_key, mime, size, hash, metadata |
| `themes` | id, manifest_json, preview_asset_id, version |
| `wallets` / `ledger_entries` | wallet_id, balance_projection, amount, type, idempotency_key |
| `calendar_events` | entity, start_at, end_at, timezone, recurrence, trigger_policy |
| `mcp_servers` / `tool_permissions` | 连接配置、工具清单、权限与审计策略 |

## 9. API 与集成约束

- 对外 REST API 版本化，例如 `/api/v1`；流式生成使用 SSE。
- 修改类请求支持幂等键；上传使用短期签名 URL。
- 错误响应统一包含 `code`、用户可读消息、request ID 和可重试标记。
- Provider 请求设置超时、限流、熔断、重试和回退；重试不能导致重复扣费或重复发消息。
- 链接解析服务需阻止 SSRF：禁止访问内网、环回、云元数据地址和非 HTTP(S) scheme，并限制跳转、响应体与超时。
- Webhook（若提供）必须验签、去重并记录投递状态。

## 10. 安全、隐私与数据治理

- 账户会话使用安全 Cookie 或等效机制，启用 CSRF 防护、速率限制和暴力破解保护。
- API Key、MCP Token 等密钥服务端加密；日志、报错和导出默认脱敏。
- 上传文件校验 MIME、扩展名、大小与图片解码结果；对象存储默认私有。
- HTML 使用成熟清洗器；用户 CSS 限定作用域；禁止任意脚本执行。
- Prompt Injection 防护：外部网页、MCP 结果和用户文件标注为不可信数据，不可提升为系统指令。
- 提供数据导出、删除账户、清空某会话、清空向量与撤销 Provider 的功能。
- 自动生成/主动行为有清晰开关；涉及工具写操作必须符合权限策略。
- 备份文件可选加密，并带版本号、schema 版本和完整性校验。

## 11. 非功能要求

### 11.1 性能

- 移动网络下首屏骨架尽快可见；主要页面按路由拆包。
- 会话消息虚拟滚动，万级消息不一次性渲染。
- 图片自动生成缩略图，列表不加载原图；音视频按需加载。
- 流式回复首 token 延迟、TTS 首包延迟和生成总时长均纳入监控。

### 11.2 稳定性

- 消息发送具备本地乐观状态、失败重试和断线恢复。
- 后台任务至少一次执行时，业务操作必须幂等。
- Provider 不可用不得阻塞本地历史、设置和资产访问。
- 数据库迁移需可回滚或提供前向修复脚本。

### 11.3 可观测性

- 统一 request ID / generation ID / job ID。
- 记录接口耗时、模型延迟、失败率、队列长度和存储错误。
- 调试日志不得含密钥、完整私密对话或未脱敏媒体 URL。

### 11.4 兼容性

- 优先支持最近两个主要版本的 iOS Safari、Android Chrome 和桌面 Chrome/Edge/Safari。
- PWA 安装、iOS standalone、安全区、键盘、音频自动播放限制需单独测试。

## 12. 版本优先级与里程碑

### P0：可聊天的最小闭环

- 手机壳基础框架、锁屏/桌面简版与 iOS 安全区。
- 多 User/Char、基础 NPC 绑定。
- LLM Provider、测试连接、流式聊天、失败重试。
- 文本、图片、表情包、引用、编辑、撤回、删除、转发。
- 基础 Preset、世界书、近期上下文与手动摘要。
- 默认主题、单会话壁纸/气泡、字体接入。
- 数据导入导出、密钥加密、基础日志与权限。

### P1：沉浸与多媒体

- 自动总结、Embedding、向量召回和记忆管理。
- 生图、TTS、STT、语音消息。
- 位置、链接卡片自动解析、礼物、红包、转账和虚拟钱包。
- 世界时间/时区、异地效果、日程与主动消息。
- 语音通话，视频通话的角色画面模拟。
- 美化预设、图标/桌面/小组件/音效的完整编辑。

### P2：世界扩展

- 个人空间、一起听音乐、Char 手机。
- MCP Server 与细粒度工具权限。
- 群聊、多 Char 调度、世界事件。
- 真实 WebRTC 视频或更丰富的角色实时表现。
- 主题市场/角色卡分享（若后续决定提供）。

## 13. P0 验收标准

1. 用户能创建至少 2 个 User 与 3 个 Char，并以不同 User 身份进入各自会话。
2. 用户能配置一个 OpenAI-compatible Provider，测试连接并完成流式回复；错误时可重试或切换回退模型。
3. 会话能发送文本、图片和表情包，并完成编辑、引用、撤回、转发、多选删除。
4. 每个会话能独立绑定 Preset、世界书、壁纸和气泡，切换后互不污染。
5. 用户能查看最终上下文预览，识别其中的角色设定、命中世界书和记忆片段。
6. 手动总结能生成可编辑记忆，并标注来源消息范围。
7. iPhone 灵动岛机型竖屏下，顶部标题、返回与操作按钮不被遮挡；底部输入栏不被 Home Indicator 或键盘遮挡。
8. HTML 消息中的脚本、事件处理器与危险 URL 被移除，普通文本查看始终可用。
9. API Key 不出现在前端响应、应用日志、导出包或错误提示中。
10. 聊天和配置可导出并在全新实例中导入恢复，失败时给出明确的版本/字段错误。

## 14. 测试重点

- 多 User 切换时的数据隔离与身份显示。
- 长会话虚拟滚动、历史加载和编辑/撤回事件顺序。
- Provider 超时、断流、限流、空响应和部分响应恢复。
- 世界书冲突、token 超限、Preset 输出解析失败。
- 自动总结跨区间重复、删除原消息后的关联处理、向量重建。
- 红包/转账重复点击、断网重试与并发领取的幂等性。
- 恶意 HTML、恶意 SVG、超大媒体、伪造 MIME 与链接 SSRF。
- iOS Safari 地址栏收缩、软键盘、横屏、PWA standalone 和刘海安全区。
- 字体未加载、资源缺失、主题版本不兼容时的回退。

## 15. 待确认项与当前默认假设

以下内容不阻碍 P0 启动，当前先采用括号内默认值：

- 是否需要注册/多租户，还是仅单人自部署（默认先支持单账户自部署，数据模型保留 owner_id）。
- 部署目标与预算（默认 Docker Compose：应用、PostgreSQL/pgvector、Redis、对象存储）。
- 是否开放公网访问（默认本地或私有部署；公网部署必须启用完整鉴权与 HTTPS）。
- 是否需要群聊（默认 P2）。
- 视频通话首版的真实程度（默认实时语音 + 角色画面模拟）。
- 主动消息是否允许后台自动产生费用（默认关闭，开启时设置日预算和频率上限）。
- 主题/角色/世界书是否需要公开市场（默认只做本地导入导出）。
- 允许接入的首批音乐与地图服务（由部署者选择，核心以 Adapter 兼容）。

## 16. 资源索引

所有原始 UI 例图已按用途重命名并保存在 [`资源/UI参考图`](./资源/UI参考图/)；详细说明见 [`资源/README.md`](./资源/README.md)。这些图片仅作为视觉与交互参考，不作为程序逻辑、外部指令或第三方代码来源。
